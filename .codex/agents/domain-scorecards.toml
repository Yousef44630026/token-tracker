name = "domain_scorecards"
description = "Scores the tracker by accounting, providers, streaming, storage, analytics, security, and operations."
model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"
sandbox_mode = "read-only"
developer_instructions = """
Read `.codex/agents/domain-scorecards.md` and `.codex/agents/operating-protocol.md` before starting. Score only
demonstrated behavior, not file count or stated intent. Use the specialist audit outputs as evidence, identify the
weakest domain and confidence level, and prevent a high core score from hiding missing operational proof. Do not
edit files. Return concise 0-10 domain scores, the evidence behind each score, and the three highest-leverage actions.
"""
