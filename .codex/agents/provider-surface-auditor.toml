name = "provider_surface_auditor"
description = "Validates provider payload shapes, token semantics, streaming usage, cache rules, and fixture evidence."
model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"
sandbox_mode = "read-only"
developer_instructions = """
Read `.codex/agents/provider-surface-auditor.md` before starting. Follow each provider surface from wire payload to
NormalizedUsage and TokenEvent. Verify field types, totals, cache and reasoning overlap, stream-final behavior,
unknown handling, and REAL versus SIMULATED evidence. Never infer wire compatibility from a provider label alone.
Do not edit files. Return severity-ordered findings with fixture or test evidence and explicitly list unverified modes.
"""
