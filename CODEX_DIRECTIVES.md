# CODEX ⇄ CLAUDE — directives de travail

**Rôles.** Claude (révision/architecture) écrit les directives ici. **Codex écrit le code** et
coche les tâches. Claude relit ensuite, vérifie la suite, et ajoute le lot suivant.
Ce fichier est notre **canal de communication** (il n'y a pas de chat direct entre agents).

## Règles dures (NE PAS violer)
1. **La suite doit rester 100 % verte.** Lancer après chaque tâche :
   `& "C:\Users\yerabhaoui\python-portable\python.exe" tests\test_*.py` (chaque test imprime `[PASS]/[FAIL]`, exit≠0 si échec).
2. **Pas de vraies credentials, pas de vrais appels payants.** Tout en **fixtures SIMULÉES**,
   marquées `_SIMULATED: true` + `_warning`, fidèles aux **formats documentés** du provider.
3. **Fail-closed.** Tout nouveau couple `(provider, token_type)` non enregistré dans
   `tracker/normalization/additivity.py` doit rester `unverified` (contribue 0 + flag) tant
   qu'il n'est pas vérifié. Enregistrer explicitement avant de compter.
4. **Ne PAS casser les invariants** INV-1..7 ni les 6 falsifieurs cœur
   (additivity_no_double_count, event_grain_no_double_count, storage_no_stored_derived_fields,
   stream_supersession_no_double_count, export_totals_match_model, propagation).
5. **Réconciliation obligatoire** : pour chaque nouvelle fixture, le test doit vérifier
   `event_contributing_tokens` et (si un total provider existe) `event_total_mismatch == 0`.
6. **Une zone à la fois** pour éviter les collisions Claude↔Codex. Si tu touches un fichier
   « cœur » (normalizer, additivity, models), note-le dans la section JOURNAL ci-dessous.

## Protocole
- Codex : prends la 1ʳᵉ tâche `[ ]`, implémente, lance la suite, passe-la à `[x]` avec une
  note (nb de fichiers/assertions, +N tests). Ajoute toute découverte en JOURNAL.
- Claude : relit, confirme le vert, et ajoute le lot suivant.

---

## OBJECTIF ACTUEL : max de tests + simulés AWS / Azure

### Lot A — AWS / Bedrock (priorité)
- [x] **A1. Bedrock ConverseStream (simulé).** ✅ Claude — `test_aws_simulated.py` Fixture d'événements stream :
  `messageStart` → `contentBlockDelta` (texte) → `contentBlockStop` → `messageStop` →
  `metadata` (avec `usage:{inputTokens,outputTokens,totalTokens}` + `metrics.latencyMs`).
  Test : `consume_stream(events, BedrockConverseAdapter(), context=, text_extractor=)` →
  complete EXACT, total réconcilie. Le `text_extractor` lit `contentBlockDelta.delta.text`.
- [ ] **A2. Bedrock InvokeModel — variantes de corps par modèle (simulé).** Fixtures réalistes
  boto3 complètes pour **Titan**, **Nova**, **Llama**, **Cohere**. Aujourd'hui l'adaptateur lit
  les en-têtes `x-amzn-bedrock-*` (model-agnostique) → garde ce chemin, mais ajoute un test par
  famille prouvant que l'en-tête donne bien input/output même quand le corps diffère.
- [x] **A3. Bedrock embeddings (Titan/Cohere embed) réaliste.** ✅ Claude — `test_aws_simulated.py` Fixture full boto3 + test :
  `BedrockEmbeddingsAdapter` → 1 quantité `embedding`, réconcilie. (Adapter déjà existant.)
- [x] **A4. Bedrock — robustesse.** ✅ Claude — `test_aws_simulated.py` Réponse sans en-tête de tokens → `raw_usage_missing` ;
  en-tête présent mais non parseable → ignoré proprement.

### Lot B — Azure (priorité)
- [x] **B1. Azure OpenAI Chat avec content-filter (simulé).** ✅ Claude — `test_azure_simulated.py` Fixture full avec
  `prompt_filter_results` + `choices[].content_filter_results` AUTOUR de `usage`. Test :
  `AzureOpenAIChatCompletionsAdapter` ignore les champs de filtre et extrait l'usage ; provider
  `azure_openai` ; réconcilie. (Vérifie que les champs Azure parasites ne cassent rien.)
- [x] **B2. Azure OpenAI Responses (simulé).** ✅ Claude — `test_azure_simulated.py` Fixture full (reasoning + cache) ; réconcilie.
- [x] **B3. Azure streaming.** ✅ Claude — `test_azure_simulated.py` Événements stream type OpenAI avec `usage` final →
  `consume_stream` → EXACT ; provider `azure_openai`.
- [ ] **B4. Azure déploiement vs modèle.** Test documentant que `model` = modèle sous-jacent
  (pas le nom de déploiement, absent du corps) ; pas de crash si `model` manquant.

### Lot C — Élargir la couverture de tests (max tests)
- [ ] **C1.** Fixture **réaliste full** pour chaque adaptateur qui n'en a pas encore
  (Mistral, Cohere, Voyage) dans `tests/fixtures/realistic/`.
- [ ] **C2.** Pour chaque provider, un test **drift** (champ renommé → `raw_usage_missing` ou
  `provider_total_mismatch`, jamais un total faux silencieux).
- [ ] **C3.** Un **slot de capture réelle** par provider (comme `gemini_generate.REAL.json` +
  `capture_*.py`), prêt à recevoir un vrai payload — sans le lancer (pas de credentials).
- [ ] **C4.** Test d'intégration **AWS+Azure dans une même trace** (multi-provider étendu) :
  total = somme, export Excel réconcilie, flags par provider corrects.

### Lot D — surfaces & providers supplémentaires (streaming + batch)
- [ ] **D1. Vertex AI streaming (simulé).** Événements stream Gemini (`usageMetadata` dans le
  chunk final) → `consume_stream(events, VertexAIGenerateContentAdapter(), context=, text_extractor=)`
  → EXACT, provider `vertex_ai`, réconcilie (input+output+thinking).
- [ ] **D2. OpenAI Batch API (simulé).** Une ligne de résultat batch a l'usage **imbriqué** :
  `{"custom_id": "...", "response": {"status_code": 200, "body": {... "usage": {...}}}}`.
  Ajouter un helper qui **déballe** `response.body` puis passe au `OpenAIChatCompletionsAdapter`
  (ou un mini-adaptateur batch). Test : N lignes batch → N events, total réconcilie. **Ne pas**
  sommer `provider_total` entre lignes — chaque ligne = 1 event.
- [ ] **D3. Streaming Cohere v2 + Mistral (simulé).** Cohere : `message-end` event avec
  `delta.usage` (tokens) ; Mistral : stream type OpenAI avec `usage` final. `consume_stream`
  → EXACT pour chacun, providers `cohere` / `mistral`, réconcilie.
- [ ] **D4. OpenAI Responses streaming (simulé).** L'event `response.completed` porte
  `response.usage`. `extract_usage_from_stream_event` doit aller chercher l'usage sous
  `response.usage` (imbriqué) → `consume_stream` → EXACT, réconcilie.
- [ ] **D5. Slots de capture réelle (sans lancer).** Pour OpenAI, Anthropic, Azure : un
  `examples/capture_<provider>.py` (clé via env, REST+urllib) + un slot
  `tests/fixtures/realistic/<provider>.REAL.json` + un `tests/test_real_payload_<provider>.py`
  qui **skip si le fichier REAL est absent** (modèle = `test_real_payload_gemini.py`). NE PAS
  exécuter (pas de credentials) — juste préparer le chemin de validation.
- [ ] **D6. Gemini/Vertex countTokens (pré-vol, simulé).** L'endpoint `countTokens` renvoie
  `{"totalTokens": N, "promptTokensDetails": [...]}`. Mapper en **estimate de pré-vol**
  (precision=estimate, usage_source=local_tokenizer ou un nouveau `provider_count_endpoint`)
  — attaché en metadata, **ne contribue pas** au total (c'est un pré-vol, pas l'appel réel).

---

## JOURNAL (qui touche quoi — éviter les collisions)
- (Codex) Verification audit from `docs/CODEX_VERIFY_PROMPT.md`: added 4 new audit tests
  (`test_categorization_matrix.py`, `test_reconciliation_audit.py`,
  `test_categorization_completeness.py`, `test_double_count_guard.py`) and
  `docs/VERIFICATION_REPORT.md`. Focused audit tests green: 4 files / 367 checks / 0 failures.
  No production counting semantics changed. Full `tests/run_all.py` was started but
  interrupted before completion, so no full-suite pass is claimed.
- (Codex) …
- (Claude) Créé ce fichier de directives + lots A/B/C/D.
- (Claude) FAIT moi-même A1/A3/A4 (`test_aws_simulated.py`) + B1/B2/B3 (`test_azure_simulated.py`) + fixture `azure_chat_content_filter.SIMULATED.json`. Suite verte : **86 fichiers / 1188 assertions**. Reste pour Codex : A2, B4, lot C, lot D.
- (Claude) Corrigé un vrai bug du registre `tracker/adapters/registry.py` : 5 adaptateurs existants (Mistral, Cohere, Voyage, Vertex AI, Bedrock Embeddings) n'étaient PAS enregistrés → `create_adapter(...)` échouait dessus. Ajouté `tests/test_registry_completeness.py` (garde-fou anti-dérive : parcourt toutes les sous-classes concrètes de `BaseAPISurfaceAdapter` et exige qu'elles soient joignables via le registre).
- (Claude) **Données RÉELLES (sans crédit API)** : `tracker/proxy/claude_code_logs.py` (nouveau, miroir de `codex_logs.py`) importe les vrais transcripts Claude Code (`~/.claude/projects/**/*.jsonl`) via le vrai `AnthropicMessagesAdapter`. Point critique trouvé et corrigé : un tour API est éclaté sur PLUSIEURS lignes JSONL (une par bloc de contenu), toutes avec le MÊME `usage` sous le même `requestId` → dédup obligatoire par `requestId` sinon double/triple comptage. Test `tests/test_claude_code_logs.py` (12 checks) le prouve. Script `examples/track_claude_code_usage.py` : **1734 vrais tours, 425 695 151 tokens réels mesurés** sur cette machine. Existe déjà côté Codex : `examples/track_codex_usage.py` (1298 events, 157M tokens).
- Suite globale actuelle : **92 fichiers / 1639 assertions / 0 échec**.

- (Claude) **Chasse aux failles + corrections (2026-07)** — revue ciblée (git indisponible, pas de diff possible) sur sécurité serveur/secrets/comptage cœur/adaptateurs/concurrence/stockage. 7 trouvailles corrigées, toutes avec test de non-régression :
  1. `tracker/adapters/registry.py` : `openai/embeddings` et `azure_openai/embeddings` existaient mais n'étaient pas enregistrés (re-dérive du bug historique) → ajoutés. **15 adaptateurs** au total désormais.
  2. `tests/test_registry_completeness.py` : le garde-fou lui-même avait un angle mort (`__subclasses__()` ne voit que les modules déjà importés) → force maintenant l'import de TOUS les modules d'`adapters/` via `pkgutil` avant de vérifier.
  3. `api/main.py` : `RecursionError` (JSON profondément imbriqué) n'était pas attrapée → séparé lecture (peut `TimeoutError`) et parsing (peut `RecursionError`), + ajout d'un timeout socket (`request_timeout_s=30`, paramètre de `create_server`) contre un client lent type slow-loris.
  4. `tracker/collector/client.py` : un envoi abandonné après `max_inflight_ms` (transport lent mais pas mort) pouvait être **renvoyé en double** ET son ack tardif était perdu → ajout de `self._abandoned` (liste de workers zombies suivis), `_reap_abandoned()` applique leur résultat s'ils finissent, leurs ids restent réservés (jamais re-envoyés, jamais évincés par `_make_room`) tant qu'ils sont potentiellement encore actifs.
  5. `tracker/adapters/cohere_chat_adapter.py` : `_field(usage,"tokens") or _field(usage,"billed_units")` traitait un `tokens: {}` présent-mais-vide comme absent → bascule silencieuse vers `billed_units`. Corrigé avec un test `is not None` explicite.
  6. `tracker/storage/file_repository.py` : troncature de fin de fichier corrompue silencieusement supprimée (aucun signal) → ajout de `logging.warning(...)` aux deux endroits (écriture ET lecture), sans changer le comportement par défaut (`recover_truncated_tail=False` reste l'échappatoire stricte).
  Suite finale : **93 fichiers / 1652 assertions / 0 échec**. Tests ajoutés/mis à jour : `test_collector_fault_injection.py` (le scénario "stuck" testait l'ANCIEN comportement buggé, réécrit pour prouver zéro double-envoi), `test_more_providers.py` (regression Cohere), `test_api_server_errors.py` (JSON profondément imbriqué -> 400, serveur reste sain).

- (Claude) **Correction supersession final-vs-final + tests de logique profonde (2026-07)** —
  1. **Bug corrigé** : `tracker/normalization/supersession.py` ne gérait que « partiel vs final » ; deux événements « final » légitimes partageant le même `request_correlation_id` (ex. livraison webhook at-least-once dupliquée) contribuaient TOUS LES DEUX → double comptage. Un seul `request_correlation_id` = une seule tentative logique (un vrai retry obtient un nouvel id, cf. `context/propagation.py.retry()`), donc un second "final" sur le même id est une mesure dupliquée, pas un appel distinct. Corrigé : `_pick_authoritative_final()` choisit le `timestamp` le plus récent (repli sur l'ordre de la liste si absent/égal, déterministe) ; tout autre "final" du groupe est maintenant superseded comme un partiel.
  2. **`tests/test_core_logic_deep.py`** (nouveau, ~24 370 vérifications) : fuzzing déterministe (seed fixe) de l'algèbre de comptage — 3000 événements aléatoires + 300 traces aléatoires vérifiés contre la formule exacte (`event_contributing_tokens`/`quantity_in_total`), plus 6 cas adverses ciblés sur `supersession.py` (deux finals, sensibilité à l'ordre, source mixte, multi-partiels, idempotence entrelacée, final avec sous-total) et 6 cas pathologiques (50 quantités, event vide, tout-exclu, zéro réel vs inconnu, entiers 10^15, négatifs rejetés).
  3. **`tests/test_realistic_scenarios_deep.py`** (nouveau) : 5 scénarios narratifs costauds avec du VRAI contenu substantiel (pas de placeholders) — A) conversation RAG 4 tours avec vrai document de politique + cache OpenAI croissant ; B) run d'agent avec 3 vrais appels d'outils + cache additif Anthropic croissant + variante max_steps_reached ; C) vrai flux de ~165 mots interrompu à 60% puis reconnecté (estimation vérifiée dans la bonne fourchette) ; D) failover inter-providers (panne OpenAI → bascule Anthropic) prouvant que les DEUX coûts comptent (pas un doublon) ; E) le bug de livraison dupliquée corrigé, avec de vrais volumes (5050 tokens).
  Suite finale : **95 fichiers / 1687 assertions / 0 échec**.

- (Claude) **AWS Bedrock validé sur du réel (2026-07)** — `examples/capture_bedrock_converse.py` (boto3, supporte Bedrock API key bearer token ET paire IAM classique) a capturé un vrai payload Converse (Nova Micro, eu-west-3, via ARN de profil d'inférence `arn:aws:bedrock:eu-west-3:...:inference-profile/eu.amazon.nova-micro-v1:0`) → `tests/fixtures/realistic/bedrock_converse.REAL.json`. Réconcilie exactement (input=10, output=4, total=14). **3e provider validé sur du réel** (Gemini, Anthropic, Bedrock). `tests/test_reconciliation_audit.py` (audit de Codex) a immédiatement détecté le nouveau fichier non mappé dans `FIXTURE_ADAPTERS` — corrigé (1 ligne ajoutée). Suite : 96 fichiers / 1700 assertions / 0 échec.

- (Claude) **Fuzzer d'intégration + bug corrigé (2026-07)** — `tests/test_random_integration_fuzz.py` (nouveau) : payloads aléatoires réalistes à travers les VRAIS adaptateurs (15/15 via le registre, total attendu calculé indépendamment à la main par provider, pas de circularité) + groupes de supersession randomisés (0-4 finals dupliqués, timestamps aléatoires/absents) + chaos du collecteur (invariant de conservation) + export CSV randomisé + payloads garbage sur tous les adaptateurs. Graine configurable via `FUZZ_SEED` env var, testé sur 9 graines différentes (~30 000 vérifications), 0 échec après correction.
  **Bug trouvé et corrigé** : `tracker/normalization/normalizer.py` — le filet de sécurité « ne lève jamais » ne couvrait que l'appel à l'adaptateur, pas la construction finale de `TokenEvent`. Un payload garbage avec un `totalTokens` non-entier passait la validation de l'adaptateur (qui ne type-check pas), puis `TokenEvent.__post_init__` levait `TypeError` (comportement voulu, INV-1) — mais cette exception s'échappait de `normalize()` non capturée. Corrigé : tout le corps de `normalize()` (adaptateur + assemblage + construction de l'event) est maintenant dans un seul bloc défensif. Suite : 97 fichiers / 0 échec.

- (Claude) **Test RAG multi-modèles (2026-07)** — `tests/test_rag_multimodel_deep.py` (nouveau) : pipeline RAG complet (embedding → vector_search → reranking → prompt_assembly → final_generation) testé sur une **matrice de 3 modèles d'embedding × 17 modèles de génération réels** (gpt-4o/mini, o1/o3-mini/o4-mini, azure, claude opus/sonnet/haiku, gemini 2.5 pro/flash/flash-lite, nova pro/micro, mistral large/small, command-r-plus) + ratios de reasoning/thinking extrêmes (30 cas) + 80 runs randomisés bout-en-bout. Vérifie que le pipeline total = embedding + rerank + génération UNIQUEMENT (jamais l'estimation de contexte récupéré). Testé sur 6 graines (~4080 vérifications), 0 échec. Découverte au passage (pas un bug, confirmation) : Bedrock Converse ne renvoie jamais le modèle dans le corps — comportement déjà confirmé sur une vraie capture. Suite : 98 fichiers / 0 échec.

- (Claude) **Azure OpenAI Responses API validé sur du réel, nouvel endpoint unifié (2026-07)** — `examples/capture_azure_openai_responses.py` (nouveau, SDK `openai` officiel via `client.responses.create(...)`, base_url Azure `.../openai/v1`, distinct de `capture_azure_openai.py` qui parle l'ancienne route REST `/chat/completions`) a capturé un vrai payload `gpt-5-mini` : input=16, output=129 (dont reasoning=64, correctement subtotal_of output), provider_total=145, `event_contributing_tokens=145`, `event_total_mismatch=0`, aucun flag. **4e provider/surface validé sur du réel** (Gemini, Anthropic, Bedrock, Azure OpenAI Responses). Fixture : `tests/fixtures/realistic/azure_openai_responses.REAL.json`. Aussi préparé `examples/capture_azure_cohere.py` (Cohere Command R+ via Azure AI Foundry serverless) — pas encore exécuté avec de vraies credentials, en attente du déploiement côté utilisateur.
  Note opérationnelle (pas un bug code) : l'utilisateur a collé une vraie clé API Azure OpenAI en clair dans le terminal → clé signalée compromise, régénération recommandée.
  `tests/test_reconciliation_audit.py` a immédiatement détecté ce nouveau fixture non mappé (même garde-fou que Bedrock) — corrigé (import + 1 ligne dans `FIXTURE_ADAPTERS`).

- (Claude) **Azure limits/shuffle fuzz, ancré sur du réel (2026-07)** — `tests/test_azure_limits_fuzz.py` (nouveau, 1382 vérifications) : Partie 1, tenant "helpdesk" à 3 déploiements Azure (cache croissant par conversation, blocages content-filter Azure interleaved) prouvant l'isolation stricte du tag `azure_deployment` par quantité (aucune fuite croisée) sur 40 conversations. Partie 2, **fuzzer de mutation ancré sur notre propre payload réel capturé** (`azure_openai_responses.REAL.json`, gpt-5-mini) : 300 mutations à travers 9 paliers d'échelle (0 → 10^12) + 3% d'injections négatives (doivent être rejetées proprement, `normalization_error`, contribue 0, jamais de crash) — aucune perte de précision aux grandes échelles (entiers Python arbitraires). Partie 3, 3 formes adverses ciblées propres à Azure : un champ leurre `prompt_tokens` niché dans `content_filter_results` n'est JAMAIS lu comme usage réel ; un renommage hypothétique du sous-champ reasoning perd le détail mais le total haut niveau réconcilie quand même (rien n'est fabriqué) ; une sortie bloquée par content-filter avec `output_tokens=0` est traitée comme une vraie mesure (EXACT 0), pas omise. Partie 4, stress de mélange (200 événements, 5 ordres d'insertion aléatoires indépendants) prouvant la commutativité stricte des totaux. Testé sur 6 graines (seed défaut + 1/2/42/777/999999), 0 échec. Suite globale : **100 fichiers / 0 échec**.

- (Claude) **Azure OpenAI — 2 appels REELS supplementaires, cache confirme (2026-07)** — `examples/capture_azure_real_stress.py` (nouveau) :
  1. **Cache reel confirme** : meme prompt (~2324 tokens) envoye 2 fois de suite sur `gpt-5-mini` (Responses API) → appel #1 `cached_tokens=0`, appel #2 `cached_tokens=2304` (sur le meme input). Les deux reconcilent exactement (`event_total_mismatch=0`), et `CACHED_INPUT` reste bien `subtotal_of input` (exclu du total). C'est la 1re confirmation reelle de comportement de cache Azure sur ce projet (jusqu'ici seule la STRUCTURE etait validee, pas la DYNAMIQUE cache-hit).
  2. **Test content-filter — resultat contraire a l'hypothese, pas un bug** : une phrase de declenchement QA standard n'a PAS ete rejetee au niveau requete (pas de HTTP 400/`content_filter_result`) — Azure a renvoye une reponse 200 normale (`status=completed`, usage reel `input=31 output=1088(reasoning=704) total=1119`, reconciliation parfaite, aucun flag). Conclusion : sur ce deploiement, la moderation ne bloque pas au niveau prompt pour ce declencheur — le scenario "vrai blocage -> raw_usage_missing" reste NON valide sur du reel (seulement simule dans `test_azure_limits_fuzz.py` Partie 1/3a). A retenter avec un declencheur different si on veut cette preuve terrain.
  Fixtures sauvees : `azure_cache_behavior_call1/2.REAL.json`, `azure_content_filter_block_completed.REAL.json`.

- (Claude) **Registre d'adaptateurs : auto-decouverte, la classe de bug ne peut plus se reproduire (2026-07)** — `tracker/adapters/registry.py` reecrit : au lieu d'un dict maintenu a la main (casse 2 fois : Mistral/Cohere/Voyage/VertexAI/Bedrock-embeddings, puis OpenAI/Azure-embeddings), le registre force maintenant l'import de tous les modules `tracker/adapters/` (`pkgutil`) puis indexe automatiquement chaque sous-classe concrete de `BaseAPISurfaceAdapter` par `(provider, api_surface)` — memes mecanique que `test_registry_completeness.py`, mais appliquee au registre lui-meme, pas juste au test qui le verifie apres coup. Une collision de cle leve maintenant une `RuntimeError` explicite au chargement plutot que d'ecraser silencieusement. `create_adapter`/`available_adapters` gardent la meme signature publique — zero impact sur les appelants. Suite : 101 fichiers / 0 echec (2 echecs pre-existants non lies decouverts et corriges au passage, voir ci-dessous).
  **Decouverte au passage — Codex a deja avance sur l'export CSV/Excel en parallele** : `tracker/export/csv_exporter.py` expose desormais `build_metric_exports()` avec 7 feuilles derivees supplementaires (CostSummary, LatencySummary, ReliabilitySummary, CacheEfficiency, RagEfficiency, AgentEfficiency, ServiceAttribution), plus `tracker/analytics/cost.py` (`PriceTable` **entierement fourni par l'appelant**, aucun prix cable en dur — conforme a la contrainte dure "NO pricing logic anywhere" : sans price_table, tout est `unpriced`, cout=0). Ca correspond exactement au chantier "export plus clair" mis en pause plus tot avec l'utilisateur. Deux tests etaient devenus obsoletes face a ce changement legitime, corriges :
    1. `tests/test_reconciliation_audit.py` : 3 nouveaux fixtures REAL (`azure_cache_behavior_call1/2.REAL.json`, `azure_content_filter_block_completed.REAL.json`, captures de la session precedente) non mappes dans `FIXTURE_ADAPTERS` — ajoutes (`AzureOpenAIResponsesAdapter`).
    2. `tests/test_robustness_values.py` : assertion `set(paths) == {3 cles}` trop stricte, cassait des que l'export legitime en ecrit plus — changee en sous-ensemble (`{3 cles} <= set(paths)`).

- (Claude) **Points d'amelioration du code, suite (2026-07)** —
  1. `tests/_harness.py` (nouveau) : `Checker`/`make_checker()` centralise le pattern `check()`/`_failures` redondant dans ~70 fichiers de test. **Decision deliberee : pas de retro-migration en masse des 70 fichiers existants** — trop de variations locales (certains trackent aussi `_checks`, certains ont un rapport final custom) pour un rewrite mecanique sans risque, et ca collisionnerait avec les fichiers que Codex edite en parallele. Le module est pret pour les NOUVEAUX tests des maintenant.
  2. `tests/run_all.py` : ajoute un **gate ruff+black** (`_run_lint_gate`) avant les tests — CLAUDE.md l'exigeait deja mais rien ne l'appliquait automatiquement. Si ruff/black ne sont pas installes dans l'interpreteur, c'est signale `[SKIP]` explicitement (pas un succes silencieux) avec la commande pip a lancer ; s'ils sont installes et trouvent une violation, ca fait echouer la suite. `--skip-lint` disponible pour bypasser volontairement. Verifie : ni ruff ni black ne sont installes ici → skip propre confirme, 0 faux-positif introduit, suite globale toujours a 101 fichiers / 0 echec (une execution intermediaire a montre 7 echecs transitoires pendant que Codex editait des fichiers en parallele — confirmes non-lies en re-executant, pas une regression du gate).

- (Claude) **Preuve de concurrence sur de vrais threads OS (2026-07)** — `tests/test_real_concurrency.py` (nouveau, 484 verifications) : contrairement aux tests de concurrence existants qui construisent un entrelacement precis a la main, celui-ci lance de VRAIS threads OS (`ThreadPoolExecutor`) synchronises par un `threading.Barrier` (tous demarrent au meme instant, l'ordonnanceur OS choisit l'entrelacement reel). Partie 1 : 40 threads x 25 appels `normalize()` chacun (1000 appels concurrents) — verifie qu'aucun thread ne voit jamais le `trace_id` d'un autre EN PLEIN VOL (pas juste apres coup), et que chaque total par trace reconcilie exactement malgre la contention reelle. Partie 2 : 20 threads x spans parent/enfant imbriques — integrite des liens `parent_span_id` sous contention reelle. Stable sur 5 executions independantes a graines differentes. Confirme que `contextvars.ContextVar` (et non un mutable partage) isole vraiment les appelants paralleles, comme le pretend le docstring de `propagation.py` — maintenant prouve par l'execution, pas seulement par la lecture du code.
  Suite globale : **102 fichiers / 0 echec** (execution longue, ~5 min avec la charge des tests de concurrence — ajuster le timeout du runner en consequence).

- (Claude) **Decoupage du plus gros fichier de test narratif (2026-07)** — `tests/test_realistic_scenarios_deep.py` (397 lignes, 5 scenarios sans rapport entre eux dans un seul fichier) supprime et remplace par 5 fichiers autonomes, chacun avec ses propres imports/`check()`/rapport final : `test_scenario_a_rag_conversation.py`, `_b_agent_tool_calls.py`, `_c_stream_interrupted.py`, `_d_cross_provider_failover.py`, `_e_duplicate_delivery.py`. Comportement/assertions strictement identiques (copie fidele, verifie individuellement puis en suite complete avant suppression de l'original). N'a touche QUE des fichiers que Claude avait ecrits (zone perso, aucun risque de collision avec le travail Codex en cours sur `csv_exporter.py`/`fixture_manifest.py`). Suite globale : **107 fichiers / 0 echec**.

- (Claude) **Bug confirme et corrige : le chemin streaming avait le MEME faille que `normalizer.py` avant son durcissement (2026-07)** — trouve par revue approfondie ("overthink"), puis reproduit avant correction (principe : confirmer avant de fixer). `tracker/streaming/stream_consumer.py::consume_stream()` protegeait la boucle d'ingestion (`try/except` autour de `for event in events`) mais PAS la construction finale du `TokenEvent` (`tracker.complete_with_quantities(...)`/`tracker.complete(...)`, hors du bloc `try`). Un usage de flux final malforme (ex. `total_tokens: "not-a-number"`, un adaptateur passant une valeur brute sans validation, exactement le meme scenario qui avait fait planter `normalize()` avant son propre correctif) faisait remonter un `TypeError` NON capture hors de `consume_stream()`. Reproduit avant correction :
  ```
  TypeError: provider_total_tokens must be an integer or None
  ```
  **Corrige** : le `try/except` englobe maintenant TOUTE la fonction (ingestion + construction de l'event terminal), pas seulement la boucle — meme principe que le fix de `normalizer.py`. Repli sur `tracker.interrupt()` (ESTIMATE + flags), jamais un crash. Test de non-regression ajoute dans `tests/test_stream_consumer.py` ("malformed terminal usage"). Suite : **107 fichiers / 0 echec**.
  **Autres trouvailles du meme passage de revue, pas encore traitees** (severite decroissante) : `tracker/analytics/cost.py` n'a aucun test dedie (le fallback sur 11 formats de cles n'est jamais teste directement) ; `tests/test_real_concurrency.py` utilise un `threading.Barrier` SANS timeout (hang silencieux possible sur environnement contraint) ; la nouvelle collision `RuntimeError` dans `registry.py` n'est testee nulle part ; `azure_content_filter_block_completed.REAL.json` (fixture reelle) contient la vraie sortie du modele a un prompt de test de synthese de drogue, jamais inspectee apres capture.

- (Claude) **Suite du passage de revue "overthink" (2026-07)** — les 3 pistes restantes traitees :
  1. **Timeout ajoute sur `threading.Barrier`** dans `tests/test_real_concurrency.py` (Parties 1 et 2) : `barrier.wait(timeout=30)` + capture de `threading.BrokenBarrierError`, avec un `check()` explicite qui echoue bruyamment si un thread timeout au lieu de laisser la suite entiere pendre indefiniment sur un environnement contraint.
  2. **Test de collision ajoute** dans `tests/test_registry_completeness.py` : deux classes factices revendiquant le meme `(provider, api_surface)` prouvent que `_discover_adapters()` leve bien un `RuntimeError` descriptif (branche du refactor de registre jamais exercee jusqu'ici), et que le registre reel reste utilisable apres.
  3. **`tracker/analytics/cost.py` : la piste s'est resolue toute seule.** En ecrivant les tests, decouverte que Codex a **supprime le module cost/PriceTable en parallele** (remplace par `ObservationContract` dans `csv_exporter.py`/`excel_exporter.py`, deja synchronises entre eux). Le fichier de test ecrit pour `cost.py` a ete supprime (module inexistant desormais) plutot que de resister a une decision architecturale prise ailleurs. **Exemple concret du risque de collision sans git** deja signale : changement invisible tant qu'on n'a pas tente d'importer le module.
  4. **Fixture reelle `azure_content_filter_block_completed.REAL.json` inspectee** : le modele a correctement REFUSE (francais, ressources d'aide, aucun contenu dangereux) — 704 tokens de reasoning pour la deliberation de refus. Rien a corriger, fixture saine a garder.
  Suite globale : **107 fichiers / 0 echec**.

- (Claude) **Dernier point du passage de revue (2026-07)** — `tracker/analytics/exactness.py` inspecte : c'est un shim de compatibilite de 5 lignes (re-exporte `build_coverage_exactness` de `coverage.py`), **rien dans le code de production ne l'importe actuellement**. Pas assez de substance pour un fichier de test dedie — 2 assertions ajoutees a `tests/test_coverage_exactness_numbers.py` (deja le test de `coverage.py`) : le shim re-exporte le MEME objet fonction (pas une copie divergente), et produit une sortie identique. Suite globale : **107 fichiers / 0 echec**. Le passage de revue "overthink" est maintenant clos : 1 vrai bug (crash streaming), 3 durcissements de test (barrier timeout, collision registre, shim exactness), 1 fausse alerte resolue par le travail parallele de Codex (cost.py supprime), 1 verification de fixture reelle (saine).

- (Claude) **Gate ruff/black active pour de vrai, premiere fois du projet (2026-07)** — ruff+black installes par l'utilisateur, premiere execution reelle du gate ecrit precedemment (jusque-la toujours en mode skip). Resultat initial : **1260 violations**, mais tres concentrees : 965/1260 etaient juste "ligne trop longue" a la limite de 100 car. (le style narratif du projet la depasse naturellement). Decision (validee avec l'utilisateur) : limite remontee a **140** dans `pyproject.toml` (`[tool.black]`/`[tool.ruff]` line-length) plutot que de reformater ~965 lignes a la main pour coller a une limite arbitraire. `black .` (221 fichiers) + `ruff check . --fix` (285 corrections auto : imports mal tries, `Optional[X]`→syntaxe moderne, imports depreciees) ont ramene le total a 21 erreurs. Traitees une par une :
  - **B023** (`test_rag_multimodel_deep.py`) : faux positif verifie (la closure `_ctx()` est definie ET appelee dans la MEME iteration, jamais stockee) → `noqa` documente sur la ligne exacte.
  - **B905** (`test_csv_excel_export.py`) : `zip(header, r, strict=True)` — vrai renforcement (garde-fou d'integrite ligne/en-tete CSV↔Excel).
  - **B007 x3** (`tracker/analytics/agent.py`, `tracker/proxy/claude_code_logs.py`, `test_random_integration_fuzz.py`) : variables de boucle inutilisees, renommees en `_x` ou boucle changee en `.values()`.
  - **UP042 x6** (StrEnum) : **deliberement laisse en violation ignoree**, pas corrige. `class X(str, Enum)` → `StrEnum` change silencieusement `str(X.MEMBER)` (retourne juste la valeur au lieu de `"X.MEMBER"`) — risque reel sur un projet qui serialise beaucoup de flags d'enum. Ignore documente dans `pyproject.toml` avec la raison exacte, pas juste supprime silencieusement.
  - **E501 x10 restants** (>140 car. meme apres le bump) : messages de `check()` wrappes manuellement sur 2 lignes, aucun changement de comportement.
  Resultat final : **`ruff check .` → "All checks passed!"**, **`black --check .` → 221 fichiers conformes**. Premiere fois que l'exigence "ruff+black clean" de CLAUDE.md est reellement verifiee (et non juste affirmee) depuis le debut du projet. Suite complete relancee avec le gate REELLEMENT actif (pas skip) : **107 fichiers / 0 echec**.

- (Claude) **Revue critique de la logique/pertinence des metriques analytics + 2 corrections (2026-07)** — revue rigoureuse de tous les modules `tracker/analytics/*.py`. Trouvailles majeures corrigees :
  1. **`coverage.py::exactness_ratio`** : formule `exact / (exact + estimate)` excluait `unknown` de son propre denominateur — une trace a 90% de quantites inconnues pouvait afficher "100% exact" tant que la petite portion connue etait parfaite. Corrige : `exactness_ratio` utilise maintenant TOUTES les quantites (`exact / quantity_count`), c'est le nouveau chiffre affiche en tete. L'ancienne formule reste disponible sous `known_exactness_ratio`, explicitement documentee comme un angle plus etroit, jamais le chiffre principal. Test de regression ajoute dans `test_coverage_exactness_numbers.py` (1 exact + 9 unknown → `exactness_ratio=0.1`, pas 1.0).
  2. **`agent.py::tokens_per_successful_agent_run`** : le numerateur (`agent_tokens`, TOUS les runs) et le denominateur (`successful_runs`, uniquement le COMPTE des runs reussis) portaient sur des populations differentes — un run echoue tres couteux gonflait silencieusement le cout moyen affiche pour un run reussi. Exemple concret : 1 run reussi a 100 tokens + 1 run echoue a 100 000 tokens → ancien resultat 100 100 (faux). Corrige : totaux par run construits a partir des spans `agent_step` (qui portent `agent_run_id`), numerateur = somme des SEULS runs reussis. Limite documentee : les tokens d'appels d'outils (spans `tool`, sans `agent_run_id`) restent dans le total global de la trace mais ne sont pas attribuables a un run precis, donc exclus de cette repartition par run plutot que mal-attribues. Nouveau test dedie `test_agent_metrics_regression.py` (reproduit exactement le scenario adverse, prouve 100.0 et pas 100 100.0).
  3. **Autres trouvailles documentees mais NON corrigees** (severite moindre, a la discretion du prochain passage) : `fresh_input_tokens` (cache.py) ne veut pas dire la meme chose selon que le provider utilise un cache en subtotal (OpenAI) ou additif (Anthropic), sans le signaler ; `retrieval_waste_ratio`/`duplicate_context_hash_count` (rag.py) cadrent un comportement RAG normal (reranking, retrieval stable) comme un defaut ; `reliability.py`/`latency.py`/`service_attribution.py` dependent presque entierement du dictionnaire optionnel `observation`, jamais peuple par les helpers `agent_tracker.py`/`rag_tracker.py` du projet ni par 100/107 fichiers de test — verifie par grep : `success_rate` affiche silencieusement 100% par defaut plutot que "inconnu" quand rien n'a ete mesure, ce qui est le meme type de zero-de-confiance qu'INV-6 interdit explicitement au niveau des tokens, non applique ici ; `_cloud()` (service_attribution.py) fusionne Gemini direct et Vertex AI sous `"gcp"` alors que ce sont deux surfaces de facturation distinctes.
  Suite globale : **109 fichiers / 0 echec**, ruff+black toujours clean (1 exception de ligne ajoutee pour `powerbi_exporter.py` : une formule DAX imbriquee dans un template, wrapper la ligne aurait corrompu la syntaxe generee — documentee dans `pyproject.toml`).
  **Decouverte au passage** : Codex a ajoute un nouvel exporteur `tracker/export/powerbi_exporter.py` (+ `test_powerbi_export.py`) en parallele, non revu ici.

- (Claude) **Les 3 dernieres trouvailles de la revue metriques, corrigees (2026-07)** —
  1. **`cache.py::fresh_input_tokens`** : lisait `TokenType.INPUT` directement, ce qui signifiait "input complet incluant le cache" pour OpenAI (subtotal) mais "input frais uniquement" pour Anthropic (additif) — meme nom de champ, deux sens differents selon le provider, invisible en sortie. Corrige : derive maintenant de `prompt_input_tokens - cache_read_tokens - cache_creation_tokens`, coherent quel que soit le style d'additivite. Verifie : OpenAI (input brut=1000, cache=400) donne desormais fresh=600 (avant : 1000, faux) ; Anthropic donne fresh=600 aussi. Au passage, `verified_cache_read_tokens`/`verified_cache_creation_tokens` etaient des doublons exacts de `cache_read_tokens`/`cache_creation_tokens` (meme calcul, param par defaut deja `include_unverified=False`) — simplifies en alias au lieu de recalculer. Test `test_cache_fresh_tokens_regression.py`.
  2. **`rag.py`** : `retrieval_waste_ratio` renomme en `retrieved_not_injected_ratio`, `duplicate_context_hash_count` renomme en `repeated_context_hash_count` — le pipeline RAG de ce projet modelise explicitement une etape `reranking` dont le role EST de rejeter des candidats retrouves ; nommer ca "gaspillage"/"doublon" presupposait un defaut la ou c'est souvent le comportement attendu. Purement un renommage, aucun changement de calcul.
  3. **`reliability.py::success_rate`** : ne peut plus lire 100% par defaut quand rien n'a ete mesure. Nouvelle fonction `_is_judged()` distingue les evenements ayant un vrai signal operationnel (status/http_status/provider_error_code) de ceux qui n'en ont aucun (`observation={}`, le cas par defaut — confirme par grep que `agent_tracker.py`/`rag_tracker.py` ne renseignent jamais ces champs). `success_rate`/`error_rate` sont maintenant calcules sur la population JUGEE uniquement, et valent `None` (pas un faux 100%/0%) quand rien n'est mesurable. Nouveaux champs `judged_event_count`/`unmeasured_event_count`/`unmeasured_rate` pour la transparence. Test `test_reliability_unmeasured_regression.py` (2 scenarios : 5 evenements sans aucune donnee -> `success_rate=None` ; 1 echec juge + 4 non-mesures -> `success_rate=0.0` sans etre dilue par les non-mesures).
  Suite globale : **111 fichiers / 0 echec**, ruff+black clean.
  4. **`service_attribution.py::_cloud()`** : supprime l'entree `"gemini": "gcp"` de `_CLOUD_BY_PROVIDER` — Gemini direct (cle API, Google AI Studio) est une surface de facturation/auth distincte de Vertex AI, souvent meme pas rattachee a un projet GCP (vrai des propres captures reelles de ce projet). Vertex AI continue de s'attribuer correctement a `"gcp"` ; Gemini direct retombe maintenant sur son propre nom de provider (`"gemini"`) au lieu d'un cloud fabrique. Test `test_service_attribution_cloud_regression.py`.
  **La revue critique des metriques (6 trouvailles au total) est maintenant integralement corrigee**, aucune laissee de cote. Suite globale finale : **112 fichiers / 0 echec**, ruff+black clean.

- (Claude) **`powerbi_exporter.py` : 3 bugs deja corriges ailleurs, reapparus par duplication de code, + 1 nouveau (2026-07)** — nouvelle passe de revue rigoureuse sur l'exporteur Power BI de Codex (~950 lignes, jamais revu). Ce fichier reimplemente ses propres versions de `_cloud_provider`/`_is_error`/logique de cache au lieu de reutiliser `service_attribution.py`/`reliability.py`/`cache.py` deja corriges — mes 3 fix precedents ne le protegeaient pas. 4 corrections :
  1. **`_cloud_provider()`** : meme conflation Gemini/Vertex AI sous `"gcp"`, corrigee identiquement (entree supprimee, commentaire renvoyant vers le fix jumeau).
  2. **`error_count`/Success Rate DAX** : meme faux 100% de succes par defaut. Nouvelle colonne `measured` (0/1, meme heuristique que `_is_judged` de reliability.py) exportee dans `fact_token_events` ET agregee dans `fact_service_daily` (`measured_count`). DAX `Success Rate`/`Error Rate` divisent maintenant par `[Measured Events]`, plus par `[Total Events]`.
  3. **`Tokens Per Successful Event` DAX** : meme bug numerateur/denominateur que `tokens_per_successful_agent_run`. Nouvelle mesure `Successful Contributing Tokens = CALCULATE([Total Contributing Tokens], error_count=0)`, utilisee comme numerateur.
  4. **NOUVEAU — `Cache Hit Rate` DAX double-comptait le cache au denominateur** pour les providers OpenAI-style (`input_tokens` brut inclut deja le cache) : `DIVIDE([Cached], [Input]+[Cached])` sous-estimait le vrai taux (28,6% calcule au lieu de 40% reel sur un cas verifie a la main). Nouvelle colonne `fresh_input_tokens` (event-grain, meme formule que le fix de `cache.py` : `quantity_in_total` total moins cache verifie) ; DAX corrigee en `DIVIDE([Cached], [Fresh]+[Cached])`.
  Test `test_powerbi_exporter_regression.py` (10 verifications, les 4 corrections prouvees individuellement). Data dictionary et README mis a jour pour documenter `measured`/`fresh_input_tokens`. Aucune regression sur `test_powerbi_export.py` existant (31 checks, tous verts).
  **Le tres avoir a l'esprit** : ce pattern (logique dupliquee entre `service_attribution.py`/`reliability.py`/`cache.py` et `powerbi_exporter.py`) signifie qu'un futur fix cote analytics ne se propagera PAS automatiquement ici — a surveiller si d'autres exporteurs (html_report.py, non revu) dupliquent la meme logique.
  Suite globale : **113 fichiers / 0 echec**, ruff+black clean.

- (Claude) **`html_report.py` : 1 faille d'echappement HTML corrigee (2026-07)** — bonne nouvelle d'abord : contrairement a `powerbi_exporter.py`, ce fichier reutilise DIRECTEMENT les fonctions analytics deja corrigees (`build_reliability_summary`, `build_cache_summary`, etc.) — aucun bug duplique a corriger ici, les 6 fixes precedents s'y appliquent automatiquement. Un seul probleme trouve : `_rows_table()` construisait l'attribut `<tr class="status-...">` directement depuis `row.get("status")` SANS `html.escape()`, contrairement a toutes les autres interpolations du meme fichier (`_value()`, `_status_badge()` echappent deja). Pas exploitable via les appelants actuels (litteraux fixes "pass"/"warn"/"fail"/None), mais `status` peut remonter jusqu'a `event.observation`, que n'importe quel proxy/collecteur peut peupler depuis une reponse provider externe. Corrige : `html.escape(str(status_value))`. Test `test_html_report_escaping_regression.py` (valeur adverse `"><script>alert(1)</script>` prouvee neutralisee). Aucune regression sur `test_trust_reporting.py` (17 checks).
  Note relevance mineure (non corrigee, a la discretion) : le rapport HTML par-trace inclut systematiquement une section "Provider Validation Matrix"/"Readiness Overview" qui reflete l'etat de couverture des tests INTERNES du projet, sans rapport avec la trace affichee — verifie que ca ne lit rien sur disque (dict statique), donc pas de risque de plantage, juste un melange de portee (rapport operationnel vs statut QA interne).
  Suite globale : **114 fichiers / 0 echec**, ruff+black clean.

- (Claude) **`tracker/proxy/privacy.py` : audit de confidentialite corrige dans les deux sens (2026-07)** — nouvelle cible de revue, enjeu eleve (le projet interdit explicitement le stockage de credentials). Deux trouvailles verifiees concretement (pas de speculation) :
  1. **Faux negatifs graves** : une cle au format Azure exacte (meme forme que celle qui a fuite en clair plus tot dans cette session) et une cle d'acces AWS type passaient l'audit avec `passed: True` — aucun pattern ne couvrait ces formats.
  2. **Faux positifs** : du texte 100% legitime et sans secret (`"error_message": "Authorization failed: invalid credentials"`, `"auth_method": "oauth"`) faisait echouer l'audit — les patterns confondaient "mentionne un mot lie a l'authentification" avec "contient reellement un secret".
  Corrige : chaque pattern porte maintenant une **severite** (`info` = mention de vocabulaire, ne fait plus echouer l'audit seule ; `secret`/`error` = valeur de credential ou store corrompu, fait echouer). 3 nouveaux patterns `secret` ajoutes : cle Azure (run alphanumerique contigu 80-100 car., distinct des uuid4().hex a 32 car. deja utilises dans ce projet), cle d'acces AWS (`AKIA[0-9A-Z]{16}`), cle API Google (`AIza...`). `render_privacy_audit()` affiche desormais la severite de chaque trouvaille. Test `test_privacy_audit_severity_regression.py` (8 verifications : les 2 faux positifs corriges, les 2 faux negatifs corriges, le vrai secret existant toujours detecte). Aucune regression sur `test_privacy_audit.py` existant (6 checks, tous verts — verifie que `finding["kind"]` reste inchange, seule `severity` est un ajout).
  Suite globale : **115 fichiers / 0 echec**, ruff+black clean.

- (Claude) **Proxy HTTP : un en-tete de tracage malforme cassait le vrai appel API (2026-07)** — revue de `tracker/proxy/server.py` (surface la plus sensible cote securite). Le proxy lui-meme est bien durci (binding loopback force, pas d'injection d'hote, limites de body, redaction du prompt). UN bug concret et reproductible trouve, a l'intersection de deux fichiers :
  1. **`tracker/context/headers.py::extract()`** violait son contrat documente ("return None on incomplete identity, never raise"). Le garde utilisait la truthiness (`not values[field]`), qu'une valeur d'en-tete composee uniquement d'espaces passe (une string d'espaces est truthy). Cette valeur blanche etait ensuite passee a `TraceContext(...)`, dont `__post_init__` rejette un id whitespace-only → `extract()` LEVAIT au lieu de retourner None. Reproduit : `X-TokenTracker-Trace-Id: "   "` + Span/Correlation valides → `ValueError`. Corrige : garde `.strip()` (les 3 champs requis, verifie independamment ; un id avec du vrai contenu entoure d'espaces reste accepte).
  2. **`server.py::_measurement()`** appelle `extract_context()`, et `_measurement` s'execute HORS du try qui protege l'appel upstream (le try commence apres). Donc ce `ValueError` remontait non capture → cassait le VRAI appel provider, violant l'invariant explicite du proxy (ligne 559 : "observation must never break the API call"). Un appelant cross-service avec un en-tete de tracage legerement malforme cassait l'appel API reel. Corrige en defense-en-profondeur : `_measurement()` est maintenant enveloppe dans un try/except qui degrade toute defaillance de mesure en "ne rien enregistrer" (tous les chemins aval geraient deja `measurement is None`), au lieu de laisser une erreur d'observabilite avorter l'appel proxifie.
  Test `test_headers_blank_identity_regression.py` (7 verifications). Aucune regression sur `test_headers_roundtrip.py`/`test_context_headers.py`/`test_real_call_proxy.py`. Le fix de `headers.py` protege TOUS les appelants de `extract()`, pas seulement le proxy.
  Suite globale : **116 fichiers / 0 echec**, ruff+black clean.

- (Claude) **Revue storage/collector/quality (2026-07)** — `tracker/storage/` (verrou par chemin, cache invalide par signature taille+mtime, reparation de tail documentee) et `tracker/collector/client.py` (logique abandoned/zombie deja durcie lors d'un passage anterieur, dedup borne, aucune re-emission) : **inspectes, aucun bug** — solides. Un bug concret trouve et corrige dans `tracker/proxy/quality.py` :
  **`_three_bullets_exact()` cherchait le mojibake `"â€¢"`** (les octets UTF-8 de `•` U+2022, E2 80 A2, mal redecodes en Latin-1) au lieu du vrai bullet Unicode `"•"` que ses fonctions soeurs `_four_bullets`/`_five_bullets` utilisent. Consequence reproduite : une reponse avec de vrais bullets `•` etait comptee a 0 → le check qualite "Workspace read-only context" echouait a tort (faux negatif sur une reponse modele correcte). Corrige : `"•"` (verifie : 3 vrais bullets → pass, dash/star toujours acceptes, mauvais compte toujours rejete, bout-en-bout via `check_prompt_output`). Test `test_quality_bullets_regression.py`. `report.py` inspecte aussi — pas de bug (agregation defensive, isinstance partout).
  Suite globale : **117 fichiers / 0 echec**, ruff+black clean.

## À FAIRE remonter à Claude (questions / décisions)
- (Codex écris ici toute ambiguïté ou décision d'additivité incertaine.)
